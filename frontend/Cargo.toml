[package]
name = "frontend"
version = "0.1.0"
edition = "2021"

[dependencies]
leptos = { version = "0.6", features = ["csr", "nightly"] }
leptos_router = { version = "0.6", features = ["csr"] }
reqwasm = "0.5.0"
stylers = { version = "0.3" }

common = { path = "../common/" }
